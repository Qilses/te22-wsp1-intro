{%extends "layout.njk"%}
{%block content%}
<main>
    <h1>{{ title }}</h1>
    <form action = "/search" method="get">
        <input type="text" 
        name="q"
        
        
        ><button type="submit">search</button>
    </form>
    {% if query %}
        <p>Du sökte efter {{ query }}</p>
        <p>WOW you found me :D</p>
        <p>{{names}}</p>

    {% endif %}
    {% if query == names %}
             
        
    {% endif %}
    
    <p><a href="/">tillbaka till home</a></p>   
         {% for name in names %}

        <ul>
             <li><p>{{name}}</p></li>
        </ul>
        
        {% endfor %}

</main>
{%endblock%}